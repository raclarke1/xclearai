---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Free AI Readiness Assessment" description="Find out how much time and money AI automation could save your business. Free assessment for Utah businesses.">
  <section class="page-hero">
    <h1>Free AI Readiness Assessment</h1>
    <p>Discover how much time and money AI could save your business</p>
  </section>

  <section class="assessment-content">
    <div class="container">

      <div class="assessment-grid">
        <div class="assessment-form-wrapper">
          <h2>Get Your Custom AI Report</h2>
          <p>Answer a few quick questions and we'll send you a personalized breakdown of where AI can make the biggest impact in your business.</p>

          <form id="assessment-form" class="assessment-form">
            <div class="form-group">
              <label for="name">Your Name *</label>
              <input type="text" id="name" name="name" required />
            </div>

            <div class="form-group">
              <label for="email">Business Email *</label>
              <input type="email" id="email" name="email" required />
            </div>

            <div class="form-group">
              <label for="company">Company Name *</label>
              <input type="text" id="company" name="company" required />
            </div>

            <div class="form-group">
              <label for="phone">Phone Number</label>
              <input type="tel" id="phone" name="phone" />
            </div>

            <div class="form-group">
              <label for="industry">Industry *</label>
              <select id="industry" name="industry" required>
                <option value="">Select your industry</option>
                <option value="legal">Legal / Law Firm</option>
                <option value="accounting">Accounting / Bookkeeping</option>
                <option value="real-estate">Real Estate</option>
                <option value="medical">Medical / Dental</option>
                <option value="ecommerce">eCommerce / Retail</option>
                <option value="construction">Construction / Trades</option>
                <option value="other">Other</option>
              </select>
            </div>

            <div class="form-group">
              <label for="employees">Number of Employees *</label>
              <select id="employees" name="employees" required>
                <option value="">Select range</option>
                <option value="1-10">1-10</option>
                <option value="11-25">11-25</option>
                <option value="26-50">26-50</option>
                <option value="51-100">51-100</option>
                <option value="101-200">101-200</option>
                <option value="200+">200+</option>
              </select>
            </div>

            <div class="form-group">
              <label>Which tasks eat the most time? (Select all that apply)</label>
              <div class="checkbox-grid">
                <label class="checkbox-label">
                  <input type="checkbox" name="pain_points" value="email" />
                  <span>Email management</span>
                </label>
                <label class="checkbox-label">
                  <input type="checkbox" name="pain_points" value="scheduling" />
                  <span>Scheduling & booking</span>
                </label>
                <label class="checkbox-label">
                  <input type="checkbox" name="pain_points" value="data-entry" />
                  <span>Data entry & paperwork</span>
                </label>
                <label class="checkbox-label">
                  <input type="checkbox" name="pain_points" value="customer-support" />
                  <span>Customer support / FAQs</span>
                </label>
                <label class="checkbox-label">
                  <input type="checkbox" name="pain_points" value="lead-followup" />
                  <span>Lead follow-up</span>
                </label>
                <label class="checkbox-label">
                  <input type="checkbox" name="pain_points" value="reporting" />
                  <span>Reporting & analytics</span>
                </label>
                <label class="checkbox-label">
                  <input type="checkbox" name="pain_points" value="invoicing" />
                  <span>Invoicing & billing</span>
                </label>
                <label class="checkbox-label">
                  <input type="checkbox" name="pain_points" value="document-processing" />
                  <span>Document processing</span>
                </label>
              </div>
            </div>

            <div class="form-group">
              <label for="hours">Estimated hours/week your team spends on these tasks?</label>
              <select id="hours" name="hours_per_week">
                <option value="">Select estimate</option>
                <option value="5-10">5-10 hours</option>
                <option value="10-20">10-20 hours</option>
                <option value="20-40">20-40 hours</option>
                <option value="40+">40+ hours</option>
              </select>
            </div>

            <div class="form-group">
              <label for="tools">What tools do you currently use? (CRM, accounting software, etc.)</label>
              <textarea id="tools" name="current_tools" rows="3" placeholder="e.g., QuickBooks, Salesforce, Google Workspace..."></textarea>
            </div>

            <button type="submit" class="btn btn-primary btn-large">Get My Free AI Assessment →</button>
            <p class="form-note">No obligation. We'll email your personalized report within 24 hours.</p>
          </form>
        </div>

        <div class="assessment-benefits">
          <div class="benefit-card">
            <div class="benefit-icon">📊</div>
            <h3>Personalized AI Scorecard</h3>
            <p>See exactly where AI can help your specific business — not generic advice.</p>
          </div>
          <div class="benefit-card">
            <div class="benefit-icon">⏱️</div>
            <h3>Time Savings Estimate</h3>
            <p>Get a realistic projection of hours your team could save each week.</p>
          </div>
          <div class="benefit-card">
            <div class="benefit-icon">💰</div>
            <h3>ROI Breakdown</h3>
            <p>See the dollar impact — what automation could mean for your bottom line.</p>
          </div>
          <div class="benefit-card">
            <div class="benefit-icon">🎯</div>
            <h3>Priority Roadmap</h3>
            <p>A ranked list of quick wins and long-term opportunities specific to your industry.</p>
          </div>

          <div class="social-proof">
            <h3>Utah businesses are already automating</h3>
            <div class="stat-grid">
              <div class="stat">
                <span class="stat-number">15+</span>
                <span class="stat-label">Hours saved per week (avg)</span>
              </div>
              <div class="stat">
                <span class="stat-number">3x</span>
                <span class="stat-label">Faster response times</span>
              </div>
              <div class="stat">
                <span class="stat-number">40%</span>
                <span class="stat-label">Less manual data entry</span>
              </div>
            </div>
          </div>
        </div>
      </div>

    </div>
  </section>

  <script>
    document.getElementById('assessment-form')?.addEventListener('submit', async (e) => {
      e.preventDefault();
      const form = e.target;
      const formData = new FormData(form);
      
      // Collect pain points checkboxes
      const painPoints = [];
      form.querySelectorAll('input[name="pain_points"]:checked').forEach(cb => {
        painPoints.push(cb.value);
      });

      const data = {
        name: formData.get('name'),
        email: formData.get('email'),
        company: formData.get('company'),
        phone: formData.get('phone'),
        industry: formData.get('industry'),
        employees: formData.get('employees'),
        pain_points: painPoints,
        hours_per_week: formData.get('hours_per_week'),
        current_tools: formData.get('current_tools'),
        submitted_at: new Date().toISOString()
      };

      // For now, mailto fallback — replace with API endpoint when ready
      const subject = encodeURIComponent(`AI Assessment Request: ${data.company}`);
      const body = encodeURIComponent(
        `New AI Assessment Request\n\n` +
        `Name: ${data.name}\n` +
        `Email: ${data.email}\n` +
        `Company: ${data.company}\n` +
        `Phone: ${data.phone || 'N/A'}\n` +
        `Industry: ${data.industry}\n` +
        `Employees: ${data.employees}\n` +
        `Pain Points: ${painPoints.join(', ')}\n` +
        `Hours/Week on Manual Tasks: ${data.hours_per_week}\n` +
        `Current Tools: ${data.current_tools || 'N/A'}\n`
      );

      window.location.href = `mailto:ryan@xclearnetworks.com?subject=${subject}&body=${body}`;
      
      // Show thank you
      form.innerHTML = `
        <div class="thank-you">
          <h2>🎉 Assessment Requested!</h2>
          <p>Thanks, ${data.name}! We'll analyze your business and send your personalized AI readiness report within 24 hours.</p>
          <p>Keep an eye on <strong>${data.email}</strong> for your results.</p>
        </div>
      `;
    });
  </script>
</Layout>

<style>
  .assessment-content {
    padding: 4rem 0 6rem;
  }

  .assessment-grid {
    display: grid;
    grid-template-columns: 1.2fr 1fr;
    gap: 3rem;
    max-width: 1100px;
    margin: 0 auto;
  }

  .assessment-form-wrapper h2 {
    margin-bottom: 0.5rem;
  }

  .assessment-form-wrapper > p {
    color: var(--text-secondary, #666);
    margin-bottom: 2rem;
  }

  .form-group {
    margin-bottom: 1.5rem;
  }

  .form-group label {
    display: block;
    font-weight: 600;
    margin-bottom: 0.5rem;
    font-size: 0.95rem;
  }

  .form-group input,
  .form-group select,
  .form-group textarea {
    width: 100%;
    padding: 0.75rem 1rem;
    border: 1px solid var(--border, #ddd);
    border-radius: 8px;
    font-size: 1rem;
    background: var(--bg-secondary, #f9f9f9);
    color: var(--text-primary, #111);
    transition: border-color 0.2s;
  }

  .form-group input:focus,
  .form-group select:focus,
  .form-group textarea:focus {
    outline: none;
    border-color: #2563eb;
    box-shadow: 0 0 0 3px rgba(37, 99, 235, 0.1);
  }

  .checkbox-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 0.5rem;
    margin-top: 0.5rem;
  }

  .checkbox-label {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-weight: 400 !important;
    cursor: pointer;
    padding: 0.4rem;
    border-radius: 6px;
    transition: background 0.2s;
  }

  .checkbox-label:hover {
    background: var(--bg-secondary, #f0f0f0);
  }

  .btn-large {
    width: 100%;
    padding: 1rem 2rem;
    font-size: 1.1rem;
    font-weight: 700;
    border: none;
    border-radius: 10px;
    background: linear-gradient(135deg, #2563eb, #7c3aed);
    color: white;
    cursor: pointer;
    transition: transform 0.2s, box-shadow 0.2s;
  }

  .btn-large:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 25px rgba(37, 99, 235, 0.3);
  }

  .form-note {
    text-align: center;
    font-size: 0.85rem;
    color: var(--text-secondary, #888);
    margin-top: 0.75rem;
  }

  .benefit-card {
    padding: 1.5rem;
    border-radius: 12px;
    background: var(--bg-secondary, #f8f9fa);
    border: 1px solid var(--border, #e5e7eb);
    margin-bottom: 1rem;
  }

  .benefit-icon {
    font-size: 1.5rem;
    margin-bottom: 0.5rem;
  }

  .benefit-card h3 {
    font-size: 1.1rem;
    margin-bottom: 0.3rem;
  }

  .benefit-card p {
    font-size: 0.9rem;
    color: var(--text-secondary, #666);
    margin: 0;
  }

  .social-proof {
    margin-top: 2rem;
    padding: 1.5rem;
    border-radius: 12px;
    background: linear-gradient(135deg, rgba(37, 99, 235, 0.05), rgba(124, 58, 237, 0.05));
    border: 1px solid rgba(37, 99, 235, 0.15);
  }

  .social-proof h3 {
    font-size: 1rem;
    margin-bottom: 1rem;
    text-align: center;
  }

  .stat-grid {
    display: grid;
    grid-template-columns: 1fr 1fr 1fr;
    gap: 1rem;
    text-align: center;
  }

  .stat-number {
    display: block;
    font-size: 1.5rem;
    font-weight: 800;
    background: linear-gradient(135deg, #2563eb, #7c3aed);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .stat-label {
    font-size: 0.75rem;
    color: var(--text-secondary, #666);
  }

  .thank-you {
    text-align: center;
    padding: 3rem 1rem;
  }

  .thank-you h2 {
    margin-bottom: 1rem;
  }

  @media (max-width: 768px) {
    .assessment-grid {
      grid-template-columns: 1fr;
    }
    .checkbox-grid {
      grid-template-columns: 1fr;
    }
    .stat-grid {
      grid-template-columns: 1fr;
      gap: 0.5rem;
    }
  }
</style>
